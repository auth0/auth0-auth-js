name = "auth0-server-js-test"
main = "src/index.ts"
compatibility_date = "2026-03-04"
compatibility_flags = ["nodejs_compat"]

[alias]
# Replace msw/node with a Workers-compatible implementation that patches
# globalThis.fetch instead of using Node.js HTTP interceptors (node:https),
# which are not available in the Cloudflare Workers runtime.
"msw/node" = "./src/test-utils/mock-http"
